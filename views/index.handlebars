<header class="center-align"><h1>Burgers</h1></header>
<div class="container">
<div class="row"><div class="col s6">
<h4>Available Burgers</h4>
<ul>
    {{#each burgers}}
        {{#unless devoured}}
            {{> burgers/burger-block devoured=false}}
        {{/unless}}
    {{/each}}
</ul>
</div>
<div class="col s6">
<h4>Eaten Burgers</h4>

<ul>
    {{#each burgers}}
        {{#if devoured}}
            {{> burgers/burger-block devoured=true}}
        {{/if}}
    {{/each}}
</ul>
</div>
</div>
<div class="row">
    <div class="col s3">

    </div><div class="col s6 newburger">
<h4 class="addnewtitle">Add A New Burger</h4>
<form class="create-form">
    <div class="form-group">
        <label class="darken"for="bu">Burger Name:</label>
        <input type="text" id="bu" name="name">
    </div>
    <button class="btn-floating btn-large waves-effect waves-light red"type="submit" id="submit"><i class="material-icons">add</i></button>
</form>
</div>
<div class="col s3">

</div>
</div>
</div>
<footer class="center-align"><h1>Thanks For Dining With Us!</h1></footer>